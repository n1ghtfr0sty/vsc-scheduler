# 2026-03-03

## Security Updates
- Upgraded `sql.js` to `better-sqlite3` for native SQLite disk reads/writes.
- Upgraded `bcryptjs` to native `bcrypt`.
- Configured `connect-sqlite3` for persistent session storage.
- Added `express-rate-limit` middleware on auth login to stop brute-forcing.

## UI Updates
- Replaced plain text entity names across the app with clickable router links to detail pages.
- Location names now link to Google Maps.
- Discovered and added missing backend `/api/coaches/:id` and `/api/opponents/:id` endpoints + frontend sub-views.

## Results
- Package architecture is now safe from memory-driven DoS vulnerabilities.
- Event-loop unblocked during password hashing.
- Updated `GEMINI.md`.
- Enhanced discoverability via entity linking throughout the platform.
