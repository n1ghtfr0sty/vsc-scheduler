# 2026-03-03

## Security Updates
- Upgraded `sql.js` to `better-sqlite3` for native SQLite disk reads/writes.
- Upgraded `bcryptjs` to native `bcrypt`.
- Configured `connect-sqlite3` for persistent session storage.
- Added `express-rate-limit` middleware on auth login to stop brute-forcing.

## Results
- Package architecture is now safe from memory-driven DoS vulnerabilities.
- Event-loop unblocked during password hashing.
- Updated `GEMINI.md`.
